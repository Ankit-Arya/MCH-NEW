# =========================================================
# MCH Inspection Platform - .gitignore
# FastAPI + Vue + PostgreSQL + Redis + Celery + MinIO + Docker
# =========================================================

# ---------------------------------------------------------
# Environment / secrets
# ---------------------------------------------------------
.env
.env.*
!.env.example
*.env
*.key
*.pem
*.crt
*.p12
*.pfx
secrets/
secret/
credentials/

# ---------------------------------------------------------
# Python / FastAPI
# ---------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
pythonenv*
venv/
.venv/
env/
ENV/
backend/.venv/
backend/venv/
backend/env/

# Python build/package files
build/
dist/
*.egg-info/
.eggs/
*.egg
pip-wheel-metadata/

# Python test/cache/coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
.mypy_cache/
.pyre/
.ruff_cache/
.dmypy.json
dmypy.json
.cache/

# FastAPI local runtime files
*.log
logs/
backend/logs/
backend/tmp/
backend/temp/

# ---------------------------------------------------------
# Alembic / database caution
# ---------------------------------------------------------
# Keep migration scripts committed.
# Do NOT ignore backend/alembic/versions/*.py
# Ignore local SQLite/dev databases only, if any.
*.sqlite
*.sqlite3
*.db

# ---------------------------------------------------------
# Node / Vue / Vite
# ---------------------------------------------------------
node_modules/
frontend/node_modules/

# Build output
frontend/dist/
frontend/.vite/
dist/
.vite/

# npm/yarn/pnpm logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Optional package-manager caches
.pnpm-store/
.yarn/cache/
.yarn/unplugged/
.yarn/build-state.yml
.yarn/install-state.gz

# ---------------------------------------------------------
# Frontend local files
# ---------------------------------------------------------
frontend/.env
frontend/.env.*
!frontend/.env.example
frontend/coverage/
frontend/.nyc_output/

# ---------------------------------------------------------
# Docker / local volumes / generated runtime data
# ---------------------------------------------------------
# Keep Dockerfiles, docker-compose.yml, and docker-compose.override.yml committed
# if they are part of the project workflow.

# Local database/media/cache volumes if mounted in project folder
postgres_data/
pgdata/
redis_data/
minio_data/
minio/
data/minio/
data/postgres/
data/redis/

# Backups generated locally
backups/
backup/
*.dump
*.sql
*.sql.gz
*.backup

# ---------------------------------------------------------
# Uploaded/generated media and reports
# ---------------------------------------------------------
uploads/
media/
storage/
reports/generated/
exports/
*.pdf.tmp
*.xlsx.tmp

# Do not ignore documentation PDFs intentionally committed by you.
# If generated docs become too large, move them to releases/artifacts.

# ---------------------------------------------------------
# MinIO local files
# ---------------------------------------------------------
.minio.sys/

# ---------------------------------------------------------
# IDE / editor files
# ---------------------------------------------------------
.vscode/
!.vscode/extensions.json
!.vscode/settings.example.json
.idea/
*.iml
*.swp
*.swo
*~

# ---------------------------------------------------------
# OS files
# ---------------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# ---------------------------------------------------------
# Windows / IIS accidental files
# ---------------------------------------------------------
web.config.bak
*.lnk

# ---------------------------------------------------------
# Certificates / HTTPS local testing
# ---------------------------------------------------------
certbot/
letsencrypt/
nginx/certs/
ssl/
certs/
!certs/README.md

# ---------------------------------------------------------
# Temporary files
# ---------------------------------------------------------
tmp/
temp/
*.tmp
*.bak
*.old
*.orig

# ---------------------------------------------------------
# Large local artifacts / archives
# ---------------------------------------------------------
*.zip
*.tar
*.tar.gz
*.tgz
*.rar
*.7z

# ---------------------------------------------------------
# Misc tooling
# ---------------------------------------------------------
.envrc
.direnv/

# ---------------------------------------------------------
# Local compose override created only for a developer machine
# ---------------------------------------------------------
# Uncomment if every developer has their own override file.
# docker-compose.override.yml
# compose.override.yml
